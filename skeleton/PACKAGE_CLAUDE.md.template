# <PACKAGE_NAME> — Claude Guide

> Стек: <STACK>
> Эталон структуры: `<REFERENCE_PATH>`

## Access Rules — что можно трогать

| Зона | Правило |
|------|---------|
| `<WATCH_DIR>/` | Основная работа. Все изменения → тесты запускаются автоматически |
| `<READONLY_ZONES>/` | **ЗАПРЕЩЕНО.** Только через build-команду |
| `config/` | Только при явном запросе пользователя |
| `package.json` | Только при явном запросе пользователя |

## Архитектура пакета

<!-- Опиши структуру директорий и ключевые инварианты -->

```
<WATCH_DIR>/
  components/   ← компоненты
  shared/       ← общие типы и константы
  index.ts      ← entry point (корневой реэкспорт)
```

## Паттерны

### Добавление нового компонента

<!-- Опиши шаги специфичные для этого пакета -->

1. Создать папку по структуре эталона (`<REFERENCE_PATH>`)
2. ...

### Тесты

```bash
<TEST_CMD> <путь-к-файлу>
```

## Эталоны (читай перед написанием кода)

| Что | Путь |
|-----|------|
| Структура компонента | `<REFERENCE_PATH>` |

## Запрещено

- <FORBIDDEN_1>
- <FORBIDDEN_2>
