# <PACKAGE_NAME> — Claude Guide

> Стек: <STACK>
> Эталон структуры: `<REFERENCE_PATH>`

## Access Rules — что можно трогать

| Зона | Правило |
|------|---------|
| `<WATCH_DIR>/` | Основная работа. Все изменения → тесты запускаются автоматически |
| `<READONLY_ZONES>/` | **ЗАПРЕЩЕНО.** Только через build-команду |
| `config/` | Только при явном запросе пользователя |
| манифест зависимостей | Только при явном запросе пользователя |

## Архитектура пакета

<!-- Опиши структуру директорий и ключевые инварианты этого стека.
     Подробности — в @.claude/docs/ARCHITECTURE.md -->

```
<WATCH_DIR>/
  <MODULE_DIR>/   ← основная единица работы
  <SHARED_DIR>/   ← общий код
  <ENTRY_POINT>   ← точка входа
```

## Паттерны

<!-- Шаги, специфичные для этого пакета. Языковые идиомы — в rules/lang/<lang>.md. -->

### Добавление новой единицы

1. Создать по структуре эталона (`<REFERENCE_PATH>`)
2. ...

### Тесты

```bash
<TEST_CMD> <путь-к-файлу>
```

## Эталоны (читай перед написанием кода)

| Что | Путь |
|-----|------|
| Структура единицы работы | `<REFERENCE_PATH>` |

## Запрещено

- <FORBIDDEN_1>
- <FORBIDDEN_2>
