# Minimal harness — рабочий пример без настройки
# Поменяй значения под свой проект и скопируй .claude/ в корень

WATCH_DIR="src"
READONLY_ZONES="dist"
TEST_CMD="npx vitest related --run"
WIKI_PATH=""           # оставь пустым если вики нет
REPO_ROOT="$(pwd)"
